# LabelLens | Layout

Use Node.js 24.12 or newer and pnpm.

## Start

```sh
pnpm install
pnpm dev
```

Open the local URL shown in the terminal.

## Build

```sh
pnpm build
```

The built files are saved in `dist/`.
